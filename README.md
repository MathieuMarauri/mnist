# Digit detection on MNIST

This repo contains code to perform digit detection on the MNIST dataset. The results are submitted to the Kaggle platform. The output is an html document generated using Quarto.